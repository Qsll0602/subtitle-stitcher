# 更新日志

## 2026-05-17 — 全面优化

### 🐛 Bug 修复
- **裁剪拖拽**：增加最小尺寸限制(10px)，边界平滑吸附取代卡死
- **拖拽排序**：等图片加载完成后再更新裁剪框，消除不同尺寸图片交换后裁剪框错位
- **重复绑定**：移除 `init()` 中冗余的 `drop` 事件监听器，消除文件重复添加

### ✨ UI 美化
- 背景升级为柔和蓝灰渐变，卡片采用双层现代阴影
- 按钮：渐变背景 + hover 上浮 + 光晕扩散效果
- 选中态外发光环、删除按钮缩放弹出动画
- 裁剪框加粗(2px)+发光、拖拽手柄 hover 放大
- 响应式优化：桌面/大屏/移动端三档适配

### 🧹 清理
- CSS 删除 `.controls` 等 30+ 行死代码，修复 `.result-container` 自相矛盾样式
- JS 消除 `processPastedFile` / `processFiles` 重复代码，提取共享方法
- 魔法数字（50/25/0.92）提取为类常量
- 移除 5 个从未使用的属性和空壳方法 `handleKeyDown`
